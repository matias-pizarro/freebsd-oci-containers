FROM ghcr.io/matias-pizarro/freebsd-oci-containers/freebsd-base:runtime-14.snap

RUN /usr/bin/env pkg install -yr FreeBSD-ports bash \
    && /usr/bin/env pkg install -yr FreeBSD-base FreeBSD-zfs \
    && rm -f /var/cache/pkg/* \
    && rm -rf /var/db/pkg/*

CMD ["/bin/sh"]