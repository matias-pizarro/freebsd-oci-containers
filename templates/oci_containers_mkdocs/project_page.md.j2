# {{ details.name }}

The corresponding FreeBSD14.3p5-amd64 images are available on
[Github](https://github.com/matias-pizarro/freebsd-oci-containers/pkgs/container/freebsd-oci-containers%2F{{ project_alias }}),
[DockerHub]()
and
[Quay]().
CI/CD pipelines will soon add amd64/aarch64 support for 15.0, 14.snap, 15.snap and 16.snap.

=== "Image"

    ## Containerfile

    ```dockerfile title="images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/Containerfile"
    --8<-- "images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/Containerfile"
    ```

    ## Build the image

    ```console title="images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/build.sh"
    --8<-- "images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/build.sh:6"
    ```

    ## Test the image

    ```console title="images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/test.sh"
    --8<-- "images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/test.sh:6"
    ```

    ## Push the image

    ```console title="images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/push.sh"
    --8<-- "images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/push.sh:6"
    ```

    ## Run the full pipeline from building to pushing

    ```console title="images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/run_pipeline.sh"
    --8<-- "images/{{ reference_os_major_minor_version }}/{{ details.slug }}/{{ details.reference_project_version }}/run_pipeline.sh:6"
    ```

    ## Tags

    {% for full_image_tag, image in images.items() %}
    * {% for tag in image.supported_tags %}[{{ tag }}](https://github.com/matias-pizarro/freebsd-oci-containers/blob/main/build/images/{{ image.os_major_version }}.{{ image.os_minor_version }}/{{ image.project }}/{{ image.project_version }}/Containerfile){% if not loop.last %}, {% endif %}{% endfor %}
    {% endfor %}

=== "Supported tags and respective Containerfile links"

    {% for full_image_tag, image in images.items() %}
    * {% for tag in image.supported_tags %}[{{ tag }}](https://github.com/matias-pizarro/freebsd-oci-containers/blob/main/build/images/{{ image.os_major_version }}.{{ image.os_minor_version }}/{{ image.project }}/{{ image.project_version }}/Containerfile){% if not loop.last %}, {% endif %}{% endfor %}
    {% endfor %}
